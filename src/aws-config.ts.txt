export const awsConfig = {
  region: 'us-east-1',                    // change if you used a different region
  bucketName: 'chartreview-native-files-prod',
  // We'll add Cognito and API Gateway later
};